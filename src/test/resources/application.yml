spring:
  jackson:
    deserializer:
      fail-on-unknown-properties: false

starling:
  api:
    url: ${STARLING_API_URL:http://wiremock:8080}
    token: ${STARLING_API_TOKEN:mock-token}

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html 