spring:
  jackson:
    deserializer:
      fail-on-unknown-properties: false

starling:
  api:
    url: http://localhost:${wiremock.server.port}
    token: test-token

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html 